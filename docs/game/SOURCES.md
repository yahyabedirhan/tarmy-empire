# Sources

Verbatim copies fetched 2026-09-11 from https://docs.terminal.army/ (pages: `/`, `/mechanics/`, `/commands/`, `/commander/`). The site credits its formulas to the OGame Fandom Wiki (CC-BY-SA). Kept here for private reference only; remove this folder before making the repo public. To refresh: re-fetch the page, convert with pandoc, keep the header comment.

Live rules that beat the manual: `mcp__commander__combat_rules`, `mcp__commander__codex`, `mcp__commander__changelog`.
